#include <iostream>

#include <arcc/builder/target.h>
#include <arcc/builder/builder.h>

void build(arcc::Builder* builder)
{
	//using namespace arcc;

	//auto exec = builder->add_executable("arcc")
	//	.set_cpp_standard(ETargetCppStd::Cpp14)
	//	.set_compiler_flags( "/O3" )
	//	.add_sources({
	//		"arcc/main.cc",
	//		"arcc/target.cc"
	//	});
	//	
	//auto arxd = CreateTarget("arxd");

	//target_set_compiler_flags()

	////auto exec = builder->AddExecutable()
	////	.SetOptimization(
	////   3)
}

// arcc .
// arcc_build.*
// ARCC_BUILD.*